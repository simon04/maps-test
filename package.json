{
  "name": "maps-test",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:1": "esbuild --bundle --minify --outdir=dist.ol ol.js",
    "build:2": "esbuild --bundle --minify --outdir=dist.leaflet leaflet.js --loader:.png=file"
  },
  "dependencies": {
    "leaflet": "^1.7.1",
    "ol": "^6.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.8.21"
  }
}
